void vel_tiempo(){
    //////////Calculo velocidad////////////////
  velocidadB = sqrt((2*abs(presionCF21))/1.219);
  velocidadA = sqrt(2*abs(presionSF2)/1.219);
  

  /////////////////////////////////////////
  tiempo=millis();
  
}
